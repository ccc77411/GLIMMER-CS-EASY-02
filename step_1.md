# Step 1. 指针与结构体  
## 什么是指针？  
### 1.如何在C语言中定义指针变量？指针变量的大小是固定的吗？其大小与什么有关？   
- 在变量前加以个*来表示此变量为指针变量。
- 不一定，在系统所占用的内存总线一样的情况下，指针变量的大小是固定的，因为指针变量存的是内存地址而内存地址的大小是由内存总线位数定的。  
- 所以由上述可知，指针变量的大小与总线宽度也就是多少位系统有关。  
### 2.写出以下代码的输出结果，并解释原因。  
````
int x = 10;
int* p = &x;
*p = 20;

int arr[3] = {3, 6, 9};
int *q = arr;
int y = ++*arr  +  *++q;

printf("%d %d", x, y);
````  
#### 运行结果如下图  
<img width="2026" height="1476" alt="image" src="https://github.com/user-attachments/assets/67648bee-13d1-4364-9dc1-e5ec3f0115d4" />
   
- **输出结果**：由上图可以知道这一程序的输出结果为20 10。  
- **原因**：
  - 这是因为这个程序先定义了一个整数型变量x并赋值为10，又定义了一个指针变量p，将x这一变量的内存地址赋值给了p，然后再把20赋值给了解指针后的p，因为是直接在存放x的内存地址上改值，从而让变量x的值跟着改变。  
  - 接着定义了一个数组arr以及另一个指针变量q并赋值了arr，因为y等于数组arr中的第一个数加上一之后再加上q（其实也就是arr）中的第二个数，所以y就为4+6=10，所以打印结果为20 10。 
### 3.什么是野指针？简述其危害。如何避免产生野指针？  
- **定义**：野指针就是一个未初始化的指针，指向垃圾地址，里面的值完全随机。  
- **危害**：因为指向的内存地址完全随机，所以在给其赋值时会向非法地址或者当前程序的有用地值赋值，可能会导致程序崩坏或者原有数据损坏，同时还可能有安全隐患易被攻击者在那块内存内注入危险的代码从而可能使程序被控制。  
- **避免**：在定义每一个指针变量后都要记得立即给其赋一个内存地址，不使用未初始化的指针。及时置空已经被`free()`释放的指针。同时在调用函数时，不要返回局部变量（即在j调用函数内部定义的变量）的地址。 
### 4.尝试设计一个真正有效的`swap()`函数   
- 因上题末尾已经设计过一个`swap()`函数了，这里直接照搬下来。  
````
#include<stdio.h>
void swap(int *a, int *b){//输入两个指针变量
  int temp = *a;
  *a = *b;  //直接在a和b的地址上进行值的互换
  *b = temp; 
}
````    
## 什么是结构体？  
### 1.请你完成一个PerInfo结构体的定义，成员组成如下。了解一下typedef关键字与结构体的一般用法，利用typedef为你刚刚定义的结构体取一个别名。提交最终的结构体定义。  
````
1. 个人姓名（字符型数组，长度为10个字节）
2. 性别（字符型）
3. 年龄（整型）
4. 身高（双精度浮点型）
````  
- 根据题意写出的结构体定义如下  
````
struct PerInfo {      //定义了一个PerInfo结构体
    char name[10];
    char gender;
    int age;
    double height;
};
typedef struct PerInfo p1;   //用typedef取了一个别名
````
### 2.了解并说明结构体指针的含义。  
- **含义**:结构体指针就是指向结构体的指针，存放着结构体在内存中的首地址。    
### 3.了解一下结构体的内存对齐规则，据此计算一下你刚刚定义的结构体占用字节的大小（可用`sizeof`运算符验证计算结果）。提交计算过程。(此外，你还可以尝试更改一下结构体中成员的定义顺序，看看对结构体占用字节数的影响)  
- **计算过程**：
1.首先确定这个结构体内各个成员所占内存大小，char name[10]占10字节，char gender占1字节，int age占4字节，double height占8字节。  
2.然后确定这个结构体的最大对齐数是其内部成员类型大小的最大值也就是double所对应的大小8，这里注意虽然char name[10]大小为10个字节当它本身类型为char是占1字节的。  
3.接着开始相加，10加1为11，又因为int age的对齐数是4所以要补一个字节到12，然后12加上4为16，刚好是8的倍数，所以直接相加为24，也是最大对齐数8的倍数符合规则不用部位，所以大小为24.  
- **用`sizeof`验证**  
<img width="1857" height="1432" alt="image" src="https://github.com/user-attachments/assets/58525f9a-f02c-4438-9043-9881916aa65a" />

  可以看出结果确实为24。  
- 改变成员的顺序可能会是每一个成员内存在相加的过程中所需要补齐的位数不同可能使最后结构体占用字节数发生改变。   
